function filterArray(numbers, value) {
    const newNumbers = []
    for(let i = 0; i <= numbers.length; i = ++) {
        if(numbers[i] > value) {
            newNumbers.push(numbers[i])
        }
    }


    return newNumbers
}